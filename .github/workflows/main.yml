name: ESP32 Hardware CI

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v4

    - name: Check Python
      run: python --version

    - name: Install dependencies
      run: |
        pip install robotframework
        pip install pyserial

    - name: Run Robot Test
      run: robot esp_test.robot

    - name: Upload Report
      uses: actions/upload-artifact@v4
      with:
        name: Robot-Report
        path: |
          log.html
          report.html
          output.xml
